# Kokpitim - app/ importlarina gore guncellendi (UTF-8)
#
# SÜRÜM POLİTİKASI (2026-07-15):
# Alt sınırlar (>=) yerelde DOĞRULANMIŞ sürümlerdir — o sürümde test paketi
# geçiyor. Üst sınır konmadı: Docker Python 3.11, yerel 3.13 (ortam paritesi
# yok — ayrı iş), farklı çözümlemeler olabiliyor.
#
# UYARI: bu dosya tekrarlanabilir build GARANTİ ETMEZ; iki kurulum farklı
# sürümler çekebilir. Gerçek çözüm `pip-compile` ile lock dosyası (ayrı iş).
Flask>=3.0.2
Flask-WTF>=1.2.1
Flask-SQLAlchemy>=3.1.1
sqlalchemy>=2.0.36
Flask-Migrate>=4.0.5
Flask-Login>=0.6.3
Flask-Limiter==3.5.0
Flask-Caching>=2.3.1
Flask-Babel>=4.0.0
Flask-SocketIO==5.3.5
python-socketio==5.10.0
python-dotenv>=1.0.0
pandas>=2.3.0
openpyxl>=3.1.2
python-pptx>=1.0.0
bleach>=6.3.0
sentry-sdk[flask]>=2.43.0
redis>=7.3.0
marshmallow==3.20.1
marshmallow-sqlalchemy==0.29.0
# eventlet KALDIRILDI (2026-07-15): kodda hiç import edilmiyordu
# (app/extensions.py → SocketIO async_mode="threading", gunicorn sync worker).
# eventlet 0.33.3 Python 3.12+ ile import edilemiyor (distutils kaldırıldı) —
# yani kurulumu kırıyor ama hiçbir işe yaramıyordu.
# PyJWT KALDIRILDI (2026-07-15): tek kullanıcısı app/api/auth.py idi, o da
# hiçbir yerden import edilmeyen ölü koddu → silindi. pywebpush/py-vapid
# JWT'ye bağlı değil (cryptography kullanıyor) — zincir doğrulandı.
flask-swagger-ui>=5.21.0
gunicorn>=23.0.0
flask-talisman>=1.1.0
pywebpush>=2.3.0
# 2FA — app/services/totp_service.py, app/routes/totp.py
pyotp>=2.9
qrcode>=8.0
# app/services/webhook_service.py
requests>=2.32.4
# app/tasks.py (Celery worker)
celery>=5.6.2
# app/services/anomaly_service.py, ml_service.py
numpy>=2.3.1
scikit-learn>=1.7.2
psycopg[binary]>=3.2.0
APScheduler>=3.10.0

# Kule yardımcı sistemi — YAML tur tanımları
PyYAML>=6.0
reportlab>=4.0
